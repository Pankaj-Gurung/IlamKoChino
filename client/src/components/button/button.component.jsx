import React from "react";

const Button = (props) => {
  const { children } = props;
  return (
    <>
      <div>
        <button>children</button>
      </div>
    </>
  );
};

export default Button;
